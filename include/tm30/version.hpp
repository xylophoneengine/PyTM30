#pragma once

/// @file version.hpp
/// Slice 0 placeholder - provides a single symbol so the static library
/// has at least one translation unit while the real implementation is built.
/// Replaced or extended in later slices as actual colour-science code lands.

namespace tm30 {

/// Library version string (set by CMake).
constexpr const char *version = "0.1.0";

} // namespace tm30
